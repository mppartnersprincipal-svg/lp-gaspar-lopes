node_modules/

# Fotos brutas da sessão (~518 MB) — ficam fora do repo.
# As versões otimizadas para o site estão versionadas em assets/img/.
Fotos Gaspar/
.gstack/

# Segredos locais
.env
.env.local
